## Summary

<!-- What changes and why. Link the issue if there is one. -->

## Verification

<!-- How you checked it: tests added or updated, commands run, numbers.
     For a change to a likelihood, prior or the recursions, say which paper
     and equation the new form comes from, what independent path you checked
     it against, and whether outputs or detected changepoint locations move
     (see "Changing the math" in AGENTS.md). -->

## Checklist

- [ ] CI is green on `lint`, every `test (3.x)` leg, `examples` and `build`
- [ ] Tests added or updated for behaviour changes
- [ ] `CHANGELOG.md` has an entry under `[Unreleased]`
- [ ] Docs updated (`README.md`, docstrings, `examples/`) when user-facing behaviour changed
- [ ] Review comments addressed (fixed, or answered in the thread)
